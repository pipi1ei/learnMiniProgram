<!--index.wxml-->
<view>
  <!-- 1.数据绑定 -->
  <view>hello {{ name }}</view>

  <!-- 2.列表渲染 -->
  <view wx:for="{{students}}">编号： {{item.id}}, 姓名：{{ item.name }}, 年龄： {{ item.age }}</view>

  <!-- 事件监听 -->
  <view>当前计数：{{ counter }}</view>
  <button bindtap="clickBtn" size="mini">+</button>
</view>
